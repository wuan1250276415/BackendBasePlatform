server:
  port: 8231
spring:
  application:
    name: sys
  cloud:
    nacos:
      discovery:
        server-addr: 124.221.117.35:8848
        username: nacos
        password: wuanfuck321.
        group: cloud
      config:
        import-check:
          enabled: false
        server-addr: 124.221.117.35:8848
        username: nacos
        password: wuanfuck321.
        group: cloud
        prefix: sys-online.yaml
  data:
    redis:
      host: 124.221.117.35
      port: 6379
      password: wuanfuck321.
      timeout: 50000
      database: 1

logging:
  pattern:
#    console: '%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n'
    level: '%5p [${spring.application.name},%X{X-B3-TraceId:-},%X{X-B3-SpanId:-},%X{X-Span-Export:-}]'
  level:
    web: trace
management:
  tracing:
    sampling:
      probability: 1.0
  endpoints:
    web:
      exposure:
        include: '*'
  zipkin:
    tracing:
      endpoint: http://42.194.184.206:9411/api/v2/spans

test: ${tellhow.datasource.ip}